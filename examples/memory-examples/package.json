{
  "name": "@clarity-chat/example-memory",
  "version": "1.0.0",
  "private": true,
  "description": "Comprehensive examples for @clarity-chat/memory - conversation memory and context management",
  "type": "module",
  "scripts": {
    "dev": "echo 'Choose an example to run:' && echo '  pnpm run express    - Express.js server' && echo '  pnpm run fastify    - Fastify server' && echo '  pnpm run cli        - Interactive CLI'",
    "express": "tsx memory-nodejs-express.ts",
    "fastify": "tsx memory-nodejs-fastify.ts",
    "cli": "tsx memory-cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clarity-chat/memory": "workspace:*",
    "express": "^4.21.0",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
