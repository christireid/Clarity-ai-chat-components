import { Meta } from '@storybook/blocks';
import * as CoreUtils from '@clarity-chat/react/utils';
import * as StreamableValueUtils from '@clarity-chat/react/utils/streamable-value';
import * as ChatHelpers from '@clarity-chat/react/utils/chat-helpers';
import * as StreamingParser from '@clarity-chat/react/utils/streaming-parser';
import * as PerformanceUtils from '@clarity-chat/react/utils/performance';
import * as PerformanceOptimization from '@clarity-chat/react/utils/performance-optimization';
import * as ExportUtils from '@clarity-chat/react/utils/export-utils';

<Meta title="Utilities/Overview" />

const utilityCatalog = [
  {
    title: 'Core AI Utilities',
    importPath: "@clarity-chat/react/utils",
    module: CoreUtils,
    highlights: [
      '`cn` – Tailwind-friendly classname merger',
      '`ChatConfigBuilder` – compose agent configuration objects',
      '`aggressiveCompress` / `balancedCompress` – prompt compression strategies',
      '`createHybridSearch` helpers for vector + keyword recall',
      'Rate limiting primitives (`createRateLimiter`, `enforceQuota`)',
    ],
  },
  {
    title: 'Streamable Value Helpers',
    importPath: "@clarity-chat/react/utils/streamable-value",
    module: StreamableValueUtils,
    highlights: [
      '`createStreamableValue` – author server-streamed UI payloads',
      '`createStreamableValueTransformer` – map upstream events into UI fragments',
    ],
  },
  {
    title: 'Chat Helpers',
    importPath: "@clarity-chat/react/utils/chat-helpers",
    module: ChatHelpers,
    highlights: [
      '`createUserMessage` / `createAssistantMessage` factory functions',
      '`extractToolCalls` + `hasToolCalls` for post-processing responses',
      '`truncateMessagesToTokenLimit` keeps conversations within context limits',
      '`mergeStreamingChunks` merges incremental tool outputs',
    ],
  },
  {
    title: 'Streaming Parser',
    importPath: "@clarity-chat/react/utils/streaming-parser",
    module: StreamingParser,
    highlights: [
      '`parseStreamingChunk` + `extractToolInvocation` for OpenAI compatible payloads',
      '`parseSSEDataLine` helper for low-level SSE integrations',
      '`hasToolInvocation` and `extractContentFromChunk` convenience guards',
    ],
  },
  {
    title: 'Performance Toolkit',
    importPath: "@clarity-chat/react/utils/performance",
    module: PerformanceUtils,
    highlights: [
      '`measurePerformance` – wrap expensive blocks and collect timings',
      '`Batcher`, `debounce`, `throttle`, and `lazyLoad` helpers',
      '`useMemoryLeakDetector` + `useRenderPerformance` (hook exports)',
    ],
  },
  {
    title: 'Performance Optimisation',
    importPath: "@clarity-chat/react/utils/performance-optimization",
    module: PerformanceOptimization,
    highlights: [
      '`BatchProcessor` for chunking AI workloads',
      '`PerformanceTracker` and `checkPerformanceTarget` for SLAs',
      '`calculateMessageDiff` / `calculateVisibleRange` – virtualised list helpers',
    ],
  },
  {
    title: 'Export Utilities',
    importPath: "@clarity-chat/react/utils/export-utils",
    module: ExportUtils,
    highlights: [
      '`exportToMarkdown`, `exportToHTML`, `exportToJSON`, `exportToText`',
      '`calculateAnalytics` summarises transcript usage metrics',
      '`redactSensitiveInfo` + `sanitizeMessages` for compliance exports',
    ],
  },
];

export function UtilityCard({
  title,
  importPath,
  highlights,
}: {
  title: string;
  importPath: string;
  highlights: string[];
}) {
  return (
    <div className="space-y-3 rounded-xl border border-border bg-card p-5 shadow-sm">
      <div className="space-y-1">
        <h3 className="text-lg font-semibold">{title}</h3>
        <p className="font-mono text-xs text-primary">{importPath}</p>
      </div>
      <ul className="space-y-2 text-sm text-muted-foreground">
        {highlights.map((item) => (
          <li key={item} className="flex items-start gap-2">
            <span className="mt-1 h-2 w-2 rounded-full bg-primary/70" />
            <span>{item}</span>
          </li>
        ))}
      </ul>
    </div>
  );
}

<div className="space-y-8">
  <div className="space-y-3">
    <h1 className="text-3xl font-bold">Utility Toolbox</h1>
    <p className="text-sm text-muted-foreground">
      Reusable primitives for compression, caching, streaming, performance tracking, and exporting AI outputs. Every module is framework-agnostic and tree-shakeable.
    </p>
  </div>

  <div className="grid gap-5 md:grid-cols-2">
    {utilityCatalog.map((utility) => (
      <UtilityCard key={utility.title} {...utility} />
    ))}
  </div>
</div>
