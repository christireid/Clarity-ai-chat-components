#!/usr/bin/env node
export function main(): Promise<void>;
export namespace BUILD_REPORT {
    let timestamp: string;
    let nodeVersion: string;
    let memoryLimit: string;
    let concurrency: number;
    let phases: never[];
    let errors: never[];
    let warnings: never[];
    let recommendations: never[];
    let status: string;
}
//# sourceMappingURL=build-production-fixed.d.ts.map