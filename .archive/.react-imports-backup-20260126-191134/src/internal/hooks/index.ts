/**
 * Internal Hooks
 *
 * @internal
 * These hooks are for internal use only and should NOT be imported by external consumers.
 * Use the public API exports instead (e.g., `useClarityChat` instead of `useChatEnhanced`).
 */

export {
  useChat,
  useChatEnhanced,
  type CoreMessage,
  type CoreMessageContent,
  type UseChatOptions,
  type UseChatReturn,
  type MessageRole,
} from './use-chat-enhanced'
